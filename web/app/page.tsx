'use client'
import {useState} from 'react';import {nhost} from '@/lib/nhost';import Dashboard from './dashboard'
export default function Home(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[role,setRole]=useState<'owner'|'editor'|'viewer'>('viewer'),[ready,setReady]=useState(false),[error,setError]=useState('');if(ready)return <Dashboard role={role}/>;return <main className="auth"><p className="eyebrow">AI AGENT WORKFLOW BUILDER</p><h1>Build trusted agent automations.</h1><p>Every workflow is scoped to its organization and streamed live.</p><input placeholder="Email" onChange={e=>setEmail(e.target.value)}/><input placeholder="Password" type="password" onChange={e=>setPassword(e.target.value)}/><select value={role} onChange={e=>setRole(e.target.value as 'owner'|'editor'|'viewer')}><option>owner</option><option>editor</option><option>viewer</option></select><button onClick={async()=>{const {error}=await nhost.auth.signIn({email,password});if(error)setError(error.message);else setReady(true)}}>Sign in</button>{error&&<small>{error}</small>}</main>}
